# Dev/CI tooling for the whole Python side of the monorepo (see pyproject.toml).
# Floor-pinned (>=) rather than exact-pinned: this is a fast-moving toolchain
# and floors keep CI on recent releases without a hard pin going stale mid-hackathon.
# Once the team wants reproducible CI, switch to exact pins + a lockfile (pip-compile/uv).
ruff>=0.15.0
black>=24.8.0
pytest>=8.3.0
mypy>=2.1.0
pip-audit>=2.7.0

# pydantic is a dev pin here only because packages/schemas/python/tests needs
# it to run the smoke tests against entities.py/agent_io.py. Once apps/api
# declares its own real runtime dependencies, pydantic should move there as
# a proper runtime dep (Kamogelo's call — flagged, not silently done here).
pydantic>=2.9.0

# httpx2 is what starlette's TestClient imports as of starlette 1.6 (it was
# plain `httpx` before the rename). apps/api/tests can't construct a
# TestClient without it, and CI installs this file, so it belongs here
# rather than in apps/api/requirements.txt — it's a test dependency and has
# no business in the api/worker container image. Same reasoning as the
# pydantic pin above.
#
# If a stale environment resolves starlette < 1.6, that one wants `httpx`
# instead and says so in the error. A fresh install gets 1.6+.
httpx2>=2.13.0
