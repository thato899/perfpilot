# agents/test-planner

**Owner:** Developer 1 (AI / Orchestration)

Turns target/traffic information into a structured `TestPlan` and recommends the right test type (load/stress/spike/endurance/capacity/baseline/regression) for the stated objective.

Not implemented yet — this is Phase 1 work. Full contract: [docs/agents/test-planner.md](../../docs/agents/test-planner.md). Input/output types: `packages/schemas/python/agent_io.py` (`TestPlanRequest`, `TestPlanOutput`).
